apiVersion: v1
kind: Service
metadata:
  name: mongo-svc
spec:
  type: ExternalName
  externalName: 10.1.2.6 # The connexion url
################################################################
# use mongodb://<dbuser>:<dbpassword>@mongo-svc:<port>/dbname #
##############################################################
