apiVersion: v1
kind: Service
metadata:
  name: mongo-svc
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379

##################################
# use mongodb://mongo-svc/dbname #
##################################

---
apiVersion: v1
kind: Endpoints
metadata:
  name: mongo-svc
subsets:
  - addresses:
      - ip: 10.1.2.10
    ports:
      - port: 3020
